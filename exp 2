#include <stdio.h>
#include <unistd.h>

int main()
{
    int fd[2];
    char msg[] = "Hello";

    pipe(fd);

    write(fd[1], msg, sizeof(msg));
    read(fd[0], msg, sizeof(msg));

    printf("Message: %s\n", msg);

    return 0;
}
