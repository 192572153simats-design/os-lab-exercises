#include <stdio.h>

int main()
{
    int bt[4]={5,3,8,6}, wt[4], tat[4], i;

    wt[0]=0;
    for(i=1;i<4;i++)
        wt[i]=wt[i-1]+bt[i-1];

    for(i=0;i<4;i++)
    {
        tat[i]=wt[i]+bt[i];
        printf("P%d WT=%d TAT=%d\n", i+1, wt[i], tat[i]);
    }

    return 0;
}
