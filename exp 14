#include <stdio.h>
#include <string.h>

int main() {
    char file[5][20];
    int n;

    scanf("%d",&n);

    for(int i=0;i<n;i++)
        scanf("%s",file[i]);

    for(int i=0;i<n;i++)
        printf("%s\n",file[i]);

    return 0;
}
